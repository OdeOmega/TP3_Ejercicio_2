#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;


int main() {
    int cantTest, cantPC, latencia;
    cin >> cantTest;
    for(int i = 0; i < cantTest; i++){
        cin >> cantPC;
        //tipo de estructura
        for(int j = 0; j < cantPC; j++){
            for(int k = 0; k < j; k++){
                //aca la voy almacenando
                }
            }
        }
    return 0;
}